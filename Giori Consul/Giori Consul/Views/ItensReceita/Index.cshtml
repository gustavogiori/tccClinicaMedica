@model IEnumerable<Giori_Consul.Models.ItensReceita>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Medicamentos.NomeMedicamento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReceitaMedica.Id)
        </th>

        <th></th>
    </tr>

    @foreach (var item in ViewBag.ItensReceita as List<Giori_Consul.Models.ItensReceita>)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Medicamentos.NomeMedicamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReceitaMedica.Id)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
